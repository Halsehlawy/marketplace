<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Edit listing</title>
</head>
<body>
    <div class="listing-detail-container">
        <a href="/listings/<%= foundListing._id %>" class="back-button">← Back to Listing</a>
        
        <h1>Edit <%= foundListing.title %> Listing</h1>
        <form action="/listings/<%= foundListing._id %>?_method=PUT" method="POST">
            <label for="title">Title: </label>
            <input type="text" name="title" id="title" value="<%= foundListing.title %>">

            <label for="description">Description: </label>
            <textarea name="description" id="description"><%= foundListing.description %></textarea>

            <label for="price">Price: </label>
            <input type="number" name="price" id="price" value="<%= foundListing.price %>">

            <label for="image">Image URL: </label>
            <input type="text" name="image" id="image" value="<%= foundListing.image || '' %>">

            <button type="submit" class="action-btn edit-btn">Update Listing</button>
        </form>
    </div>
</body>
</html>